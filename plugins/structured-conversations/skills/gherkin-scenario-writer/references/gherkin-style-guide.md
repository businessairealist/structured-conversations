# Gherkin Style Guide

- Prefer clear business language over implementation jargon.
- Use `Given` for relevant context, not noise.
- Use `When` for the triggering action or event.
- Use `Then` for observable outcomes.
- Split scenarios when multiple branches make one scenario hard to read.
