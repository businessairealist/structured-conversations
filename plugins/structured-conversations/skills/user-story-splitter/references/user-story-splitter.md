# User Story Splitter

Split oversized stories into smaller vertical slices.

## Guardrails

- Preserve user value in each slice.
- Avoid frontend-only or backend-only slices unless the user explicitly asks for technical task decomposition.
- Keep ordering visible.
- Convert dominant unknowns into spikes instead of pretending they are clean implementation slices.
