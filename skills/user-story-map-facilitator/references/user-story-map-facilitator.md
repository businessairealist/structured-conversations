# User Story Map Facilitator Reference


This facilitator is useful when:

- the team needs a structured workshop canvas before filling in backlog detail
- backbone activities are still being negotiated
- a walking-skeleton release needs facilitation prompts rather than final commitments

The facilitator writes `user-story-map-workshop` artifacts under:

the working directory<artifact-slug>/`
