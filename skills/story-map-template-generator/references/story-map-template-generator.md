# Story Map Template Generator Reference


This template generator is useful when:

- a team wants a repeatable workshop canvas
- the map structure is known but the actual stories are not yet captured
- example content is helpful for onboarding participants

The template generator writes `user-story-map-template` artifacts under:

the working directory<artifact-slug>/`
